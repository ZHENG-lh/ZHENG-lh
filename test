#include<stdio.h>
#include<math.h>
int main()
{
	int a = 100;
	while (a <= 200)
	{
		int b = 2;
		int r = a % b;
		while (b <=sqrt(a) && r!=0)
		{
			b = b + 1;
			r = a % b;
		}
		if (b <= sqrt(a));
		{
			printf("%d\n", a);
		}
		a = a + 1;
	}
		return 0;
}
